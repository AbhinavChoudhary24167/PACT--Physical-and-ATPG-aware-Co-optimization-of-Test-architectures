# Phase-0 problem statement

For a fixed sequential design, placement, scan-cell set, chain count and length, and ATPG test set, determine whether legal scan ordering changes estimated scan wirelength and serial-shift spatial activity. Then determine whether the trade-off survives physically realizable implementation and routing. The Phase-0 result may support or fail to support the hypothesis; metric-only toy runs do not count as research evidence.
