# Provenance policy

Raw tool stdout/stderr, version output, tool/repository commits, benchmark URL/license/SHA256, seed, configuration, and derived-to-raw links are required before a quantitative result is reported. `artifacts/manifests/` holds machine-readable manifests. External repositories are kept unmodified and excluded from this Git repository.
