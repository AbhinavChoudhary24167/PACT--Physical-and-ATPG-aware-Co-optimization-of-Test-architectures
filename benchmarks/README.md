# Benchmarks

`pact_sanity` is a self-authored pipeline debug design and must not be used as Phase-0 research evidence. Imported designs require a manifest with URL, license, SHA256, and exact transformation history.
