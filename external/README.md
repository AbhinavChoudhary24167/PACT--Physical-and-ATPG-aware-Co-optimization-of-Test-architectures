# External dependencies

Clone external repositories here without editing their sources. Record remote URL, commit, license, and clone date in `artifacts/manifests/`. The directory contents are ignored by Git.
