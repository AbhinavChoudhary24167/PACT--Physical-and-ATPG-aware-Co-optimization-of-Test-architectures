PACT repository credential and path audit

Scope:
  Every logical staging set created during the cleanup and Phase-0D Optimizer-v1 work.

Credential patterns checked:
  AWS access-key identifiers (AKIA...)
  GitHub token prefixes (ghp_, gho_, ghu_, ghs_, ghr_)
  PEM/OpenSSH private-key headers
  Quoted api-key, password, and secret assignments

Result:
  Credential findings: 0
  Private-key findings: 0

Machine-specific path review:
  Generated execution provenance and reproducibility scripts contain local /mnt/c
  and /root/pact-deps paths. These are execution context, not credentials, and were
  retained because altering frozen scientific evidence would invalidate recorded
  hashes. No home-directory secrets or credential files are included.

Large-file staging review:
  No staged file exceeded 10 MiB. The largest Optimizer-v1 evidence file was the
  564,115-byte compressed routed ODB. Large local dependencies and the raw Phase-0B
  archive remain excluded by exact ignore policy and are inventoried separately.

Whitespace review:
  Frozen generated JSON and tool logs preserve their recorded CRLF byte streams, so
  git diff --check reports carriage returns as trailing whitespace in those evidence
  files. They were not rewritten; the Optimizer-v1 manifest SHA256 values match the
  committed byte streams exactly.
